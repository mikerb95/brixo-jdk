# ═══════════════════════════════════════════════════
# Brixo API — Variables de entorno
# Copiar a .env y ajustar valores
# ═══════════════════════════════════════════════════

# ── Spring Profile ──
SPRING_PROFILES_ACTIVE=dev

# ── Database ──
DB_NAME=brixo
DB_USERNAME=brixo
DB_PASSWORD=brixo_secret
DB_PORT_EXPOSED=3306
MYSQL_ROOT_PASSWORD=root

# ── App ──
APP_PORT=8080

# ── LLM / IA ──
LLM_PROVIDER=groq
LLM_API_KEY=
LLM_MODEL=

# ── AWS S3 ──
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=brixo-images

# ── Email SMTP ──
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
